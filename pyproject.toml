[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ipl_analysis"
version = "0.1.0"
description = "IPL data analysis and feature engineering"
authors = [{name = " "}]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]